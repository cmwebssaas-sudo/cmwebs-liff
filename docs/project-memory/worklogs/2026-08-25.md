# CMWebs daily worklog — 2026-08-25

## Fixed-template tenant signature preview and cache release

### Objective

完成房客手機簽約後的完整回寫與預覽：沿用使用者提供的固定 Google Docs
合約版型，簽名寫入私有簽署文件，並讓房客在手機合約文字預覽中看到手寫簽名。

### Scope and decisions

- 保留原有手機流程：閱讀固定合約 → 同意 → 身分附件（依模式）→ 手寫簽名
  → 送出；下載／簽名／上傳仍只是 fallback。
- 固定 Google Docs 原始版型不直接修改；送出時建立私有簽署副本。
- 簽名影像只由後端依已驗證的簽署 artifact 回傳，前端只接受安全的 PNG
  base64 預覽，不把 Drive URL 或內部識別資料放入網址或瀏覽器儲存。
- 使用者明確授權部署 Apps Script 與 GitHub Pages；本次未改 Sheets、Properties、
  triggers、LINE 設定或租客資料。

### Changed files and commits

- Apps Script signature-image read/write path: `apps-script/V2_CONTRACT_DOCUMENT_SIGNING.js`
  and `apps-script/V2_TENANT_LIFF_SIGNING_SESSION.js`.
- Mobile preview and release cache path: `tenant-contract.html`,
  `tenant-bind.html`, `tenant-home.html`, `frontend-release.js`.
- Regression coverage: `tests/phase133-tenant-contract-signing-ui.test.mjs`,
  `tests/phase168-fixed-contract-template.test.mjs`,
  `tests/mobile-line-document-actions.test.mjs`.
- Commits: `b4d164d` (signature preview), `d719eb9` (release version bump),
  `9e18425` (versioned entry script URLs).

### Verification

- Focused tenant signing UI test: PASS.
- Full Node suite: PASS, `48 pass, 0 fail`.
- Project validator: PASS; 81 unique routes, 81 handlers, no duplicate
  top-level declarations, no blocking credential findings.
- `git diff --check`: PASS.
- Apps Script deployment readback: PASS, existing Web App serving Version 125.
- GitHub Pages workflow `32793428257`: PASS for build, deploy, and status report.
- Public readback: PASS, `tenant-contract.html` HTTP 200 and all three tenant
  entry pages reference `frontend-release.js?v=20260825-tenant-signature-preview-v1`.

### Unresolved / HUMAN_REQUIRED

- 未完成真實 LINE 登入的 603 房客手機驗收；目前不能宣稱房客端已確認看到
  簽名圖片。
- 直接開啟巢狀 `tenant-contract.html` 會重現 LINE 400，因為 URL 不在 LIFF
  endpoint 下；下一次測試必須從官方 LIFF 入口重新開啟，並完整關閉舊 webview
  以排除舊快取。
- tenant-contract 的獨立 deep-link redirect hardening 尚未納入本次 release。

### Exact next starting point

在真實 LINE 裝置從官方 LIFF 入口開啟 603 fixture，完成送出後確認三件事：

1. 手機顯示明確「已送出／待房東審核」成功訊息；
2. 合約文字預覽的乙方簽名欄出現手寫簽名圖片與簽署日期；
3. 私有簽署版 Google Docs 也有相同簽名回寫。

若失敗，先保留登入後的畫面、時間、錯誤訊息與 API 回應證據，再判斷是
LIFF entry、Apps Script、artifact、Drive 文件或前端快取問題；不要盲目重部署。

### Git and worktree handoff

- Isolated worktree: `/Users/hans/CMWebs/cmwebs-liff/.worktrees/fix-fixed-contract-template-20260825`
- Branch: `codex/fix-fixed-contract-template-20260825`
- HEAD at handoff: `9e18425` before this documentation handoff commit.
- Root aggregate worktree `/Users/hans/CMWebs/cmwebs-liff` remains dirty and is
  user-owned; it was not cleaned, reset, or overwritten.
- Apps Script rollback target and Pages rollback revision: `HUMAN_REQUIRED`;
  identify them before any rollback action.
