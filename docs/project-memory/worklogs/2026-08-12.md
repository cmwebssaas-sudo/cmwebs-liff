# CMWebs daily worklog — 2026-08-12

## Tenant test identity migration — Phase 147

### Objective

移除四個公開房客 HTML 頁面的 page-local LINE UID，讓 `test=1` 測試請求由
Apps Script `TEST_TENANT_LINE_UID` Script Property 解析身份，同時保留正式
LIFF profile 流程與付款回報的 Phase 146 `tenant-bind.html` gateway。

### Scope

- `tenant-payment-report.html`
- `tenant-contract.html`
- `tenant-renewal.html`
- `tenant-termination.html`
- `tests/phase147-tenant-test-identity-migration.test.mjs`

合約頁的主 `callApi`、簽署身分狀態、簽署送交狀態與附件上傳狀態共四個
tenant JSONP 邊界均在測試模式附加 `test=1`。三個續約／退租／合約主查詢
頁面則允許測試模式維持空的瀏覽器 `LINE_USER_ID`，正式模式仍要求真實 LIFF
profile 身份。

### Production boundary

本工作單元未修改 Apps Script、Script Properties、Google Sheets、付款／合約／
租客資料，未執行任何 production write、LINE 發送、`clasp` push 或 GitHub
Pages deployment。Apps Script production baseline 仍為 Version 102。

### Verification

- Phase 147 focused test：PASS（`node --test tests/phase147-tenant-test-identity-migration.test.mjs`）。
- Existing Node regression suite：PASS（`node --test tests/*.test.mjs`，18 tests）。
- Inline script syntax：PASS（四個頁面各 1 個 inline script 均可 parse）。
- UID／legacy constant scan：PASS，四頁無完整 LINE UID 與 `TEST_LINE_USER_ID`。
- `git diff --check`：PASS。
- `npm run validate`：BLOCKED BY BASELINE，clean Phase 146 implementation worktree
  沒有 `package.json`，執行時回報 `ENOENT`；未以 root 混合 dirty branch 的結果替代本分支驗證。

### Post-merge deployment and identity reconciliation

- PR #23 merged to GitHub `main` as `0bbbe06e`.
- The authorized GitHub Pages workflow `31601674513` completed successfully,
  and the four public Phase 147 tenant pages were fetched successfully.
- Read-only Apps Script verification identified the authenticated project and
  active Web App Version 102. The 42 editor files matched the 42 GitHub
  `main` Apps Script filenames after `.gs`/`.js` suffix normalization; this was
  an inventory check, not a byte-level export comparison.
- No Apps Script redeploy, Sheets/Properties/trigger change, LINE message, or
  payment action occurred in this reconciliation package. Full evidence is in
  `docs/125-PRODUCTION-IDENTITY-RECONCILIATION-2026-08-12.md`.
